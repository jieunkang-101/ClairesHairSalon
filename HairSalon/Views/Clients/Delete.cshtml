@{Layout="_Layout";}

@model HairSalon.Models.Client

<h1>Are you sure you want to delete this?</h1>

<p>@Html.DisplayFor(model => model.Name)</p>

@using (Html.BeginForm())
{
  <input type="submit" value="Delete" class = "btn btn-dark btn-large" />
}

@Html.ActionLink("Back to clients details", "Details", new {id = Model.ClientId}, new { @class = "btn btn-dark"})
